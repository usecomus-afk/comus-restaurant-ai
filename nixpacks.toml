[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd artifacts/api-server && npm install"]

[phases.build]
cmds = ["cd artifacts/api-server && npm run build"]

[start]
cmd = "cd artifacts/api-server && node dist/index.mjs"
